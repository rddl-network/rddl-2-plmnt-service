---
name: Audit

on: [push]

jobs:
  audit:
    strategy:
      matrix:
        directory: ['./', 'client']
    uses: rddl-network/github-actions/.github/workflows/audit.yaml@add-wd-variable
    with:
      working-directory: ${{ matrix.directory }}
